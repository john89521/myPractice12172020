function playTurn(player, location) {
	points = 0;
	if (location == 1) {
		point = points + 100;
	}
	return points;
}
var total = playTurn("Jai", 1);
alert(points);
// this code works; another function sample